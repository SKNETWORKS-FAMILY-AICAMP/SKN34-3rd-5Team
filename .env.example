# 관광공사 API키
KTO_TOUR_APT_KEY = ...

# 카카오 API 키
KAKAO_REST_API_KEY = ...
KAKAO_JS_API_KEY = ...

# 티맵 대중교통 혼합 API 키 (대중교통의 경우 하루 10건 호출 제한)
TMAP_API_KEY = ...

# 네이버 API키 및 ID
# (MAPS는 지도시각화 , LOCAL은 지역,장소 검색)
# (MAPS의 경우 이후 개발 주소가 생기면 수정/등록해야함)

NAVER_MAPS_ID = ...
NAVER_MAPS_API_KEY = ...

NAVER_LOCAL_CLIENT_ID = ...
NAVER_LOCAL_API_KEY = ...

# ODsay 대중교통 API 키 ( 개발 주소가 생기면 수정/등록해야함)
# ODSAY_API_KEY = ...

# ── 백엔드 (backend/.env 에 두세요 — settings.py, chat_service.py, build_index 가 그 위치를 읽음)
# OpenAI: 채팅(gpt) + 임베딩(text-embedding-3-small) 같은 키 하나로 충분
OPENAI_API_KEY = ...
# EMBEDDING_MODEL = text-embedding-3-small

# DB — 비우면 로컬 docker compose 의 db 컨테이너(mydb/myuser/db:5432)에 붙음
# 원격 DB 에 적재할 때만 채우세요
# DB_HOST = ...
# DB_PORT = 5432
# DB_NAME = ...
# DB_USER = ...
# DB_PASSWORD = ...
