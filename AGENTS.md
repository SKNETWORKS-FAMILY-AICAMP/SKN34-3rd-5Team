# 팀 GitHub 협업 규칙

사용자가 2026-09-13에 팀 Notion의 GitHub 규칙과 본문에 연결된 하위 문서를 모두 확인하고 이후 작업에 적용하도록 요청했습니다. Git 작업 전 [팀 GitHub 규칙 정리](docs/TEAM_GITHUB_RULES.md)를 읽고 적용합니다. 원문 링크와 문서 간 차이의 처리 기준도 그 문서에 기록되어 있습니다.

- 실제 코드·문서·설정 작업은 `feat/...` 브랜치에서 진행합니다. 새 작업 브랜치는 최신 `upstream/develop`으로 동기화한 `develop`에서 만듭니다. 이미 진행 중인 브랜치와 미커밋 변경을 임의로 버리거나 다시 만들지 않습니다.
- `origin`은 개인 Fork, `upstream`은 팀 공용 저장소입니다. 작업 브랜치는 개인 Fork로 Push하고, 일반 PR 대상은 팀 공용 저장소의 `develop`입니다.
- 작업 브랜치 접두어는 버그 수정·문서 작업을 포함해 항상 `feat/`입니다. 브랜치 작업명은 영어 소문자와 하이픈을 사용합니다.
- 커밋 메시지는 `type: 구체적인 한글 작업 내용` 형식이며 끝에 마침표를 붙이지 않습니다. 커밋 유형은 `feat`, `fix`, `refactor`, `docs`, `test`, `chore`입니다. 브랜치 접두어 규칙과 커밋 유형을 혼동하지 않습니다.
- 커밋·PR 하나에는 하나의 목적을 담습니다. 먼저 status/diff를 확인하고, 관계없는 변경·실제 `.env`·키·비밀번호·인증 정보·불필요한 임시 파일을 포함하지 않습니다.
- 팀장이 Review하고 Approve한 PR만 Merge하며 기본 방식은 Squash and merge입니다. PR 작성자가 임의로 Merge하지 않습니다. 리뷰 수정은 기존 작업 브랜치에 추가 커밋/Push합니다.
- 팀 공용 `main`/`develop`에 직접 Push하거나 그 브랜치에서 기능을 개발하지 않습니다. 개인 Fork의 `origin/main`/`origin/develop` 동기화는 원문의 별도 절차를 따릅니다.
- 이 문서를 읽었다는 사실 자체는 커밋·Push·PR 생성·Merge를 지금 실행하라는 요청이 아닙니다. 사용자의 작업 범위와 이미 제공된 권한에 맞춰 수행합니다.

하위 경로의 `AGENTS.md`에 있는 프레임워크 지침도 함께 적용합니다.
